{
  "name": "profinoks-hostinger",
  "version": "1.0.0",
  "description": "Profinoks website prepared for Hostinger Node.js deployment",
  "main": "server.js",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install && cd frontend && npm install && npm run build && cd .. && rm -rf dist && cp -r frontend/build dist"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.3",
    "mongodb": "^6.3.0",
    "multer": "^2.1.1",
    "path": "^0.12.7"
  }
}
